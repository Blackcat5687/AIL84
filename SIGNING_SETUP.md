# إعداد توقيع الإصدار (Release Signing) عبر GitHub Actions

تم توليد ملف مفتاح توقيع (`notelink-release.keystore`) ثابت لتطبيقك، بحيث يُوقَّع كل APK بنفس الشهادة
في كل مرة، وتُثبَّت التحديثات فوق النسخة القديمة مباشرة (بدل الحاجة لحذفها وإعادة تثبيتها).

⚠️ **ملاحظة مهمة**: هذا **لا** يزيل شاشة تحذير "مطور غير معروف" في Android/Play Protect - تلك الشاشة
مرتبطة بسمعة الشهادة عبر Google نفسها، وليست شيئاً يمكن التحكم به من جهة التوقيع وحده.

## خطوات الإعداد (مرة واحدة فقط)

1. اذهب لمستودعك على GitHub → **Settings** → **Secrets and variables** → **Actions**.
2. أضف 4 أسرار جديدة (New repository secret) بالقيم التالية بالضبط:

| اسم السر (Name) | القيمة (Value) |
|---|---|
| `NOTESLINK_KEYSTORE_BASE64` | محتوى ملف `notelink-release.keystore.b64` المرفق (انسخ محتواه كاملاً كنص واحد) |
| `NOTESLINK_KEYSTORE_PASSWORD` | `NL1e0fb22492158ce3a5ea` |
| `NOTESLINK_KEY_ALIAS` | `notelink` |
| `NOTESLINK_KEY_PASSWORD` | `NL1e0fb22492158ce3a5ea` |

3. بعد إضافة الأسرار الأربعة، أي `git push` قادم سيقوم تلقائياً ببناء **نسختين**:
   - `app-debug-apk` (كما كان سابقاً، بدون تغيير)
   - `app-release-apk` (جديد - موقّع بشهادتك الثابتة، هذا هو الذي يجب تثبيته وتحديثه دائماً من الآن فصاعداً)

## احتفظ بهذا الملف في مكان آمن

- `notelink-release.keystore` و`notelink-release.keystore.b64` هما نفس الملف (الثاني مُرمّز base64
  لتسهيل لصقه في GitHub). **لا تفقدهما ولا تشاركهما مع أي أحد** - فقدانهما يعني عدم القدرة على نشر
  تحديثات فوق أي نسخة ثُبِّتت بهذه الشهادة مستقبلاً (سيضطر المستخدمون لحذف التطبيق وإعادة تثبيته).
- كلمتا السر (`NOTESLINK_KEYSTORE_PASSWORD` و`NOTESLINK_KEY_PASSWORD`) متطابقتان هنا لتبسيط
  الإعداد، ومحفوظتان فقط في هذا الملف - لسن مخزّنتين في الكود أو في المستودع نفسه.
